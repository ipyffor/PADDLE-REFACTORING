import transforms
from transforms import functional as F
from paddle.fluid import dygraph
transforms.Normalize
import paddle
import numpy as np
paddle.fluid.enable_imperative(paddle.fluid.CPUPlace())
transforms.To
a = dygraph.to_variable(np.array(3))
print(type(a))